name: Prove OSV offline mode

on:
  pull_request:
    paths:
      - .github/workflows/security-dependencies-npm.yml
      - .github/workflows/prove-osv-offline-mode.yml
  push:
    branches: [main]
    paths:
      - .github/workflows/security-dependencies-npm.yml
      - .github/workflows/prove-osv-offline-mode.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  offline-mode-proof:
    uses: ./.github/workflows/security-dependencies-npm.yml
